import CanvasRenderer from './canvas/CanvasRenderer'
import DropZone from './canvas/DropZone'
import { deviceOptions } from './builderConfig'
export default function BuilderCanvas({ site, selectedPage, selectedSectionId, setSelectedSectionId, device, addSection, moveSection, duplicateSection, deleteSection, inlineEdit, cardAction }) {
  return <main className="min-w-0 overflow-auto bg-[radial-gradient(circle_at_top,#dbeafe,transparent_35%),#d8dee9] p-6"><div className="mb-4 flex items-center justify-between"><div><p className="text-xs font-black uppercase tracking-[0.25em] text-slate-500">Live canvas</p><h2 className="text-xl font-black text-slate-950">{selectedPage?.title}</h2></div><p className="rounded-full bg-white/70 px-3 py-1 text-xs font-bold text-slate-600 shadow">{deviceOptions[device].label} preview</p></div><div className="mx-auto transition-all duration-300" style={{ width: deviceOptions[device].width, maxWidth: '100%' }}><DropZone onDropSection={(type) => addSection(type, 0)} label="Drop a section at the top" /><div className="overflow-hidden rounded-[28px] bg-white shadow-2xl ring-1 ring-black/10"><CanvasRenderer site={site} page={selectedPage} selectedSectionId={selectedSectionId} setSelectedSectionId={setSelectedSectionId} moveSection={moveSection} duplicateSection={duplicateSection} deleteSection={deleteSection} inlineEdit={inlineEdit} cardAction={cardAction} /></div><DropZone onDropSection={(type) => addSection(type)} label="Drop a section at the bottom" /></div></main>
}
