import { useState } from 'react'
import { widgetGroups } from '../../../lib/builder/defaultBlocks'

export default function DropZone({ onDropSection, label }) {
  const [open, setOpen] = useState(false)
  const add = (type) => { onDropSection(type); setOpen(false) }
  return <div onDragOver={(event) => event.preventDefault()} onDrop={(event) => { const type = event.dataTransfer.getData('builder/section-type'); if (type) add(type) }} className="group relative my-3 rounded-2xl border-2 border-dashed border-slate-300 bg-white/60 px-4 py-3 text-center text-xs font-black uppercase tracking-wide text-slate-500 transition hover:border-sky-500 hover:bg-sky-50"><div className="flex flex-wrap items-center justify-center gap-2"><span>{label}</span><button type="button" onClick={() => setOpen((value) => !value)} className="rounded-full bg-sky-600 px-3 py-1.5 text-xs font-black text-white shadow-sm transition hover:-translate-y-0.5 hover:bg-sky-500 hover:shadow active:scale-95 focus-visible:outline focus-visible:outline-2 focus-visible:outline-sky-400">+ Add here</button></div>{open && <div className="absolute left-1/2 top-full z-50 mt-2 grid w-[min(520px,90vw)] -translate-x-1/2 grid-cols-2 gap-2 rounded-3xl border border-slate-200 bg-white p-3 text-left shadow-2xl ring-1 ring-black/5 sm:grid-cols-3"><p className="col-span-full px-2 pb-1 text-[11px] font-black uppercase tracking-[0.2em] text-sky-500">Choose what to add here</p>{widgetGroups.map((widget) => <button key={widget.type} draggable onDragStart={(event) => event.dataTransfer.setData('builder/section-type', widget.type)} onClick={() => add(widget.type)} className="rounded-2xl border border-slate-200 bg-slate-50 p-3 text-left text-xs font-black text-slate-800 transition hover:-translate-y-0.5 hover:border-sky-400 hover:bg-sky-50 hover:shadow active:scale-95"><span className="mb-1 block text-lg text-sky-600">＋</span>{widget.label}</button>)}</div>}</div>
}
