import { Button } from './ui.jsx'
import { deviceOptions } from './builderConfig'
export default function BuilderTopBar({ site, selectedPageId, setSelectedPageId, addPage, saveDraft, publish, undo, redo, device, setDevice, status, dirty }) {
  return <header className="z-50 flex h-16 items-center gap-2 border-b border-white/10 bg-slate-950 px-4 text-white shadow-2xl"><div className="mr-3 flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-2xl bg-sky-500 font-black">B</div><div><p className="text-sm font-black leading-none">Visual Builder</p><p className="text-[11px] text-slate-400">Live canvas editor</p></div></div><Button tone="ghost" onClick={() => saveDraft()}>Save Draft</Button><a className="rounded-xl bg-white/10 px-3 py-2 text-sm font-bold transition hover:bg-white/20" href="/preview" target="_blank">Preview</a><Button tone="green" onClick={publish}>Publish</Button><Button tone="ghost" onClick={undo}>Undo</Button><Button tone="ghost" onClick={redo}>Redo</Button><div className="mx-3 h-8 w-px bg-white/10" />{Object.entries(deviceOptions).map(([key, item]) => { const Icon = item.icon; return <button key={key} title={item.label} onClick={() => setDevice(key)} className={`grid h-10 w-10 place-items-center rounded-xl ${device === key ? 'bg-sky-500 text-white' : 'bg-white/10 text-slate-300 hover:bg-white/20'}`}><Icon /></button> })}<div className="ml-auto flex items-center gap-2"><select className="max-w-56 rounded-xl border border-white/10 bg-white px-3 py-2 text-sm font-semibold text-slate-900" value={selectedPageId} onChange={(event) => setSelectedPageId(event.target.value)}>{site.pages.map((page) => <option key={page.id} value={page.id}>{page.title}</option>)}</select><Button tone="blue" onClick={addPage}>Add page</Button><a className="rounded-xl bg-white/10 px-3 py-2 text-sm font-bold text-white" href="/admin/legacy-editor">Legacy editor</a><span className={`rounded-full px-3 py-1 text-xs font-bold ${dirty ? 'bg-amber-400 text-slate-950' : 'bg-emerald-500/20 text-emerald-200'}`}>{status}</span></div></header>
}
