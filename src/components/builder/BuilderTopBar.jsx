import { Button } from './ui.jsx'
import { deviceOptions } from './builderConfig'
export default function BuilderTopBar({ site, selectedPageId, setSelectedPageId, addPage, saveDraft, publish, undo, redo, device, setDevice, status, dirty, busyAction, notify }) {
  return <header className="z-50 flex h-16 items-center gap-2 border-b border-white/10 bg-slate-950 px-4 text-white shadow-2xl"><div className="mr-3 flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-2xl bg-sky-500 font-black shadow-lg shadow-sky-500/30">B</div><div><p className="text-sm font-black leading-none">Visual Builder</p><p className="text-[11px] text-slate-400">Live canvas editor</p></div></div><Button tone="ghost" loading={busyAction === 'save'} onClick={() => saveDraft()}>{busyAction === 'save' ? 'Saving…' : 'Save Draft'}</Button><a onClick={() => notify?.('Preview opened')} className="rounded-xl bg-white/10 px-3 py-2 text-sm font-bold shadow-sm transition-all hover:-translate-y-0.5 hover:bg-white/20 hover:shadow-lg active:scale-95 focus-visible:outline focus-visible:outline-2 focus-visible:outline-sky-400" href="/preview" target="_blank">Preview</a><Button tone="green" loading={busyAction === 'publish'} onClick={publish}>{busyAction === 'publish' ? 'Publishing…' : 'Publish'}</Button><Button tone="ghost" onClick={undo}>Undo</Button><Button tone="ghost" onClick={redo}>Redo</Button><div className="mx-3 h-8 w-px bg-white/10" />{Object.entries(deviceOptions).map(([key, item]) => { const Icon = item.icon; return <button key={key} title={item.label} onClick={() => { setDevice(key); notify?.(`${item.label} preview`) }} className={`grid h-10 w-10 cursor-pointer place-items-center rounded-xl shadow-sm transition-all hover:-translate-y-0.5 hover:shadow-lg active:scale-95 focus-visible:outline focus-visible:outline-2 focus-visible:outline-sky-400 ${device === key ? 'bg-sky-500 text-white shadow-sky-500/30' : 'bg-white/10 text-slate-300 hover:bg-white/20'}`}><Icon /></button> })}<div className="ml-auto flex items-center gap-2"><select className="max-w-56 cursor-pointer rounded-xl border border-white/10 bg-white px-3 py-2 text-sm font-semibold text-slate-900 shadow-sm transition focus:outline-none focus:ring-2 focus:ring-sky-400" value={selectedPageId} onChange={(event) => { setSelectedPageId(event.target.value); notify?.('Page selected') }}>{site.pages.map((page) => <option key={page.id} value={page.id}>{page.title}</option>)}</select><Button tone="blue" onClick={addPage}>Add page</Button><a className="rounded-xl bg-white/10 px-3 py-2 text-sm font-bold text-white transition-all hover:bg-white/20 active:scale-95" href="/admin/legacy-editor">Legacy editor</a><span className={`rounded-full px-3 py-1 text-xs font-bold shadow-sm ${dirty ? 'bg-amber-400 text-slate-950' : 'bg-emerald-500/20 text-emerald-200'}`}>{status}</span></div></header>
}
